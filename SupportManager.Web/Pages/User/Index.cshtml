@page
@model SupportManager.Web.Pages.User.IndexModel
@{
    ViewBag.Title = "User home";
}
<div class="row">
    <div class="col-lg-12">
        <div class="page-header">
            <h2>
                User details <small>for @Model.Data.DisplayName</small>
            </h2>
        </div>

        <dl class="dl-horizontal">
            <dt>Primary emailaddress</dt>
            <dd>@(Model.Data.PrimaryEmailAddressValue ?? "Not set")</dd>
            <dt>Primary phonenumber</dt>
            @if (Model.Data.PrimaryPhoneNumberValue != null)
            {
                <dd>@Model.Data.PrimaryPhoneNumberValue</dd>
            }
            else
            {
                <dd>Not set</dd>
            }
        </dl>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Emailaddresses</h3>
            </div>

            @if (Model.Data.EmailAddresses.Any())
            {
                <table class="table">
                    <thead>
                    <tr>
                        <th>Emailaddress</th>
                        <th>Verified?</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    @for (int i = 0; i < Model.Data.EmailAddresses.Count; i++)
                    {
                        <tr>
                            <td>@Model.Data.EmailAddresses[i].Value</td>
                            <td>@Html.Display(m => m.Data.EmailAddresses[i].IsVerified)</td>
                            <td>
                                @if (Model.Data.PrimaryEmailAddressId == Model.Data.EmailAddresses[i].Id)
                                {
                                    <span class="badge badge-primary">Primary</span>
                                }
                                else
                                {
                                    <a href="#">Set as primary</a>
                                }
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            }
            else
            {
                <div class="panel-body">
                    <div class="alert alert-warning" style="margin-bottom: 1px">
                        No emailaddress configured yet, you won't be able to receive email notifications!
                    </div>
                </div>
            }

            <div class="panel-footer">
                @Html.ActionLink("Add emailaddress", "AddEmailAddress", null, null, new { @class = "btn btn-default" })
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Phonenumbers</h3>
            </div>

            @if (Model.Data.PhoneNumbers.Any())
            {
                <table class="table">
                    <thead>
                        <tr>
                            <th>Label</th>
                            <th>Phonenumber</th>
                            <th>Verified?</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.Data.PhoneNumbers.Count; i++)
                        {
                            var phone = Model.Data.PhoneNumbers[i];
                            <tr>
                                <td>@phone.Label</td>
                                <td>@phone.Value</td>
                                <td>@Html.Display(m => m.Data.PhoneNumbers[i].IsVerified)</td>
                                <td>
                                    @if (Model.Data.PrimaryPhoneNumberId == phone.Id)
                                    {
                                        <span class="badge badge-primary">Primary</span>
                                    }
                                    else
                                    {
                                        <a href="#">Set as primary</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="panel-body">
                    <div class="alert alert-warning" style="margin-bottom: 1px">
                        No phonenumber configured yet, you won't be able to take support duty!
                    </div>
                </div>
            }

            <div class="panel-footer">
                @Html.ActionLink("Add phonenumber", "AddPhoneNumber", null, null, new { @class = "btn btn-default" })
            </div>
        </div>
    </div>
</div>
